

function background () {
    let homeJumboHeight = document.documentElement.clientHeight;
    let homeJumboWidth = document.documentElement.clientWidth;
    // homeJumboHeight = $('#homeJumbo').css('height');
    homeJumboWidth = $('#homeJumbo').css('width');
    console.log(homeJumboHeight);
};